{% extends 'base.html.twig' %}

{% block body %}
    <section class="bandeau_ref">
        <aside class="addSAButton">
            <a id="saButton" href="{{ path("nouveau_SA") }}"><center>Ajouter un nouveau SA</center></a>
        </aside>
        <div class="flex">
            <h1>Bonjour référent</h1>
            <p>Vous vous trouvez sur la page d'accueil de l'espace référent. Vous pouvez y retrouver l'ensemble de vos SA et y gérer votre plan d'action en gérant les différents SA</p>
            <p>🟢 Actif 🟠 A Installer 🔴 Maintenance ⚫ Inactif</p>
        </div>
    </section>
    <section>
        <h1>Plan d'action</h1>
        <ul class="pSA">
            {% for sa in planAction %}
                <li class="listSA">{% if sa.state == 'A_INSTALLER' %}🟠 {% elseif sa.state == 'ACTIF' %}🟢 {% elseif sa.state == 'MAINTENANCE' %}🔴 {% endif %}{{ sa.name }} {{ sa.currentRoom.getName }}</li>
                <br>
            {% endfor %}
        </ul>
        <h1>SA disponibles</h1>
        <ul class="pSA">
            {% for sa in inactive %}
                <li class="listSA">{{ sa.name }}</li>
                <br>
            {% endfor %}
        </ul>
    </section>
    <div>
        <p>copyrights mentions légales blablabla</p>
    </div>
{% endblock %}