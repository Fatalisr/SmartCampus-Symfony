{% extends 'base_referent.html.twig' %}

{% block body %}
    <link rel="stylesheet" href="{{ asset('detail_SA.css') }}">
    <script src="https://d3js.org/d3.v4.js"></script>
    <script src="{{ asset('graph.js') }}"></script>
    <div class="fse">
        <p> {{ sa.name }}</p>
        <p>{{ sa.currentRoom.name }}</p>
        {% if sa.state == 'A_INSTALLER' %}
            <p>🟠 Installation</p>
        {% elseif sa.state == 'ACTIF' %}
            <p>🟢 Actif</p>
        {% elseif sa.state == 'MAINTENANCE' %}
            <p>🔴 Maintenance</p>
        {% endif %}
        <button type="button" onclick="goHome()" id="goHomeBtn">Retour</button>

    </div>
    {% if sa.state == 'ACTIF' %}
        <section id="mainsection">
            <!-- graph temp -->
            <div class="pSA" id="pSAR">
                <div id="graph1"></div>
            </div>

            <div class="pSA"> <!-- maintenance -->
                {{ form_start(form) }}
                <div id="form_header">
                    <h1>Demande de Maintenance</h1>
                </div>
                <div class="form_line">
                    {{ form_widget(form.message) }}
                </div>
                <div id="submit_btn">
                    <button type="button" id="maint" onclick="openPopup('popupCS')">Envoyer</button>
                    <div id="popupCS" class="popup">
                        <div class="popup-content">
                            <span class="close" onclick="closePopup('popupCS')">&times;</span>
                            <span>Voullez-vous vraiment démarrer la maintenance de ce système d'acquisition</span>
                            <div>
                                <button id="non" type="button" onclick="closePopup('popupCS')">Non</button>
                                {{ form_widget(form.save) }}
                            </div>
                        </div>
                    </div>
                </div>
                {{ form_end(form) }}
            </div>

            <!-- graph CO2 -->
            <div class="pSA" id="pSAR">
                <div id="graph3"></div>
            </div>

            <!-- graph humi-->
            <div class="pSA" id="pSAR">
                <div id="graph2"></div>
            </div>

        </section>
    {% else %}
        <section id="mainsection">
            <div class="centre">
                <img src="{{ asset('images/noWifi.png') }}" alt="no data">
                <p>Aucun donnée disponible</p>
            </div>
        </section>
    {% endif %}
    <script>
        function openPopup(popupId) {
            document.getElementById(popupId).style.display = 'block';
        }

        function closePopup(popupId) {
            document.getElementById(popupId).style.display = 'none';
        }
        //Read the data
        function traitementDonneeTemp() {
            // Dimension de l'image SVG
            var marginTop = (document.getElementById("pSAR").offsetWidth)*0.05,
                marginRight = 10,
                marginBottom = (document.getElementById("pSAR").offsetWidth)*0.05,
                marginLeft= 35,
                width = (document.getElementById("pSAR").offsetWidth) - marginLeft - marginRight,
                height = ((document.getElementById("pSAR").offsetHeight)) - marginTop - marginBottom;

            // append the svg object to the body of the page
            var svg1 = d3.select("#graph1")
                .append("svg")
                .attr("width",  width )
                .attr("height", height + marginTop + marginBottom)
                .append("g")
                .attr("transform",
                    "translate(" + marginLeft + "," + marginTop + ")");

            d_array =  [];
            for (let i = 0; i < arrayTempT.length; i++)
            {
                d_array.push({ dateCapture : d3.timeParse("%Y-%m-%d %H:%M:%S")(arrayCO2T[i][0]), valeur : arrayCO2T[i][1] });
            }
            // Add X axis --> it is a date format
            x = d3.scaleTime()
                .domain([d3.min(d_array, function(d) { return d.dateCapture; }),d3.max(d_array, function(d) { return d.dateCapture; })])
                .range([ 0, width ]);
            svg1.append("g")
                .attr("transform", "translate(0," + height + ")")
                .call(d3.axisBottom(x));
            /*.attr("text-anchor", "start")
            .text("↑ Daily close ($)");*/

            // Add Y axis
            y = d3.scaleLinear()
                .domain([0,2000])
                .range([ height, 0 ]);
            svg1.append("g")
                .call(d3.axisLeft(y));

            // Add a label to the graph
            svg1.append("text")
                .attr("class", "y label")
                .attr("text-anchor", "start")
                .attr("x", -marginLeft)
                .attr("y", -5)
                .text("Température (°C)");

            // Add the line
            svg1.append("path")
                .datum(d_array)
                .attr("fill", "none")
                .attr("stroke", "blue")
                .attr("stroke-width",2)
                .attr("d", d3.line()
                    .x(function(d) { return x(d.dateCapture) })
                    .y(function(d) { return y(d.valeur) })
                )
        }
        function traitementDonneeHum() {
            // set the dimensions and margins of the graph
            var marginTop = (document.getElementById("pSAR").offsetWidth)*0.05,
                marginRight = 10,
                marginBottom = (document.getElementById("pSAR").offsetWidth)*0.05,
                marginLeft= 35,
                width = (document.getElementById("pSAR").offsetWidth) - marginLeft - marginRight,
                height = ((document.getElementById("pSAR").offsetHeight)) - marginTop - marginBottom;

            // append the svg object to the body of the page
            var svg1 = d3.select("#graph2")
                .append("svg")
                .attr("width",  width )
                .attr("height", height + marginTop + marginBottom)
                .append("g")
                .attr("transform",
                    "translate(" + marginLeft + "," + marginTop + ")");

            d_array =  [];
            for (let i = 0; i < arrayHumT.length; i++)
            {
                d_array.push({ dateCapture : d3.timeParse("%Y-%m-%d %H:%M:%S")(arrayCO2T[i][0]), valeur : arrayCO2T[i][1] });
            }
            // Add X axis --> it is a date format
            x = d3.scaleTime()
                .domain([d3.min(d_array, function(d) { return d.dateCapture; }),d3.max(d_array, function(d) { return d.dateCapture; })])
                .range([ 0, width ]);
            svg1.append("g")
                .attr("transform", "translate(0," + height + ")")
                .call(d3.axisBottom(x));
            /*.attr("text-anchor", "start")
            .text("↑ Daily close ($)");*/

            // Add Y axis
            y = d3.scaleLinear()
                .domain([0,2000])
                .range([ height, 0 ]);
            svg1.append("g")
                .call(d3.axisLeft(y));

            // Add a label to the graph
            svg1.append("text")
                .attr("class", "y label")
                .attr("text-anchor", "start")
                .attr("x", -marginLeft)
                .attr("y", -5)
                .text("Humidité (%)");

            // Add the line
            svg1.append("path")
                .datum(d_array)
                .attr("fill", "none")
                .attr("stroke", "blue")
                .attr("stroke-width",2)
                .attr("d", d3.line()
                    .x(function(d) { return x(d.dateCapture) })
                    .y(function(d) { return y(d.valeur) })
                )
        }
        function traitementDonneeCO2() {
            // set the dimensions and margins of the graph
            var marginTop = (document.getElementById("pSAR").offsetWidth)*0.05,
                marginRight = 10,
                marginBottom = (document.getElementById("pSAR").offsetWidth)*0.05,
                marginLeft= 35,
                width = (document.getElementById("pSAR").offsetWidth) - marginLeft - marginRight,
                height = ((document.getElementById("pSAR").offsetHeight)) - marginTop - marginBottom;

            // append the svg object to the body of the page
            var svg1 = d3.select("#graph3")
                .append("svg")
                .attr("width",  width )
                .attr("height", height + marginTop + marginBottom)
                .append("g")
                .attr("transform",
                    "translate(" + marginLeft + "," + marginTop + ")");

            d_array =  [];
            for (let i = 0; i < arrayCO2T.length; i++)
            {
                d_array.push({ dateCapture : d3.timeParseT("%Y-%m-%d %H:%M:%S")(arrayCO2[i][0]), valeur : arrayCO2T[i][1] });
            }
            // Add X axis --> it is a date format
            x = d3.scaleTime()
                .domain([d3.min(d_array, function(d) { return d.dateCapture; }),d3.max(d_array, function(d) { return d.dateCapture; })])
                .range([ 0, width ]);
            svg1.append("g")
                .attr("transform", "translate(0," + height + ")")
                .call(d3.axisBottom(x));
            /*.attr("text-anchor", "start")
            .text("↑ Daily close ($)");*/

            // Add Y axis
            y = d3.scaleLinear()
                .domain([0,2000])
                .range([ height, 0 ]);
            svg1.append("g")
                .call(d3.axisLeft(y));

            // Add a label to the graph
            svg1.append("text")
                .attr("class", "y label")
                .attr("text-anchor", "start")
                .attr("x", -marginLeft)
                .attr("y", -5)
                .text("CO2 (ppm)");

            // Add the line
            svg1.append("path")
                .datum(d_array)
                .attr("fill", "none")
                .attr("stroke", "blue")
                .attr("stroke-width",2)
                .attr("d", d3.line()
                    .x(function(d) { return x(d.dateCapture) })
                    .y(function(d) { return y(d.valeur) })
                )
        }

        // Données du jour
        donneesCO2T = '{{ donneesCO2T }}';
        donneesCO2T = donneesCO2T.replace(/&quot;/ig,'\"');
        donneesHUMT = '{{ donneesHUMT }}';
        donneesHUMT = donneesHUMT.replace(/&quot;/ig,'\"');
        donneesTEMPT = '{{ donneesTEMPT }}';
        donneesTEMPT = donneesTEMPT.replace(/&quot;/ig,'\"');
        //Données de ll'heure
        donneesCO2H = '{{ donneesCO2H }}';
        donneesCO2H = donneesCO2H.replace(/&quot;/ig,'\"');
        donneesHUMH = '{{ donneesHUMH }}';
        donneesHUMH = donneesHUMH.replace(/&quot;/ig,'\"');
        donneesTEMPH = '{{ donneesTEMPH }}';
        donneesTEMPH = donneesTEMPH.replace(/&quot;/ig,'\"');
        // Données de la semaine
        donneesCO2LW = '{{ donneesCO2LW }}';
        donneesCO2LW = donneesCO2LW.replace(/&quot;/ig,'\"');
        donneesHUMLW = '{{ donneesHUMLW }}';
        donneesHUMLW = donneesHUMLW.replace(/&quot;/ig,'\"');
        donneesTEMPLW = '{{ donneesTEMPLW }}';
        donneesTEMPLW = donneesTEMPLW.replace(/&quot;/ig,'\"');


        monJsonT = JSON.parse(donneesCO2T);
        console.log(monJsonT);
        arrayCO2T = [];
        arrayHumT = [];
        arrayTempT = [];
        for(i = 0 ; i < monJsonT.length ; i++)
        {
            if(monJson[i]["nom"] == "co2")
            {
                arrayCO2T.push([monJsonT[i]["dateCapture"],monJsonT[i]["valeur"]]);
            }
            if(monJson[i]["nom"] == "hum")
            {
                arrayHumT.push([monJsonT[i]["dateCapture"],monJsonT[i]["valeur"]]);
            }
            if(monJson[i]["nom"] == "temp")
            {
                arrayTempT.push([monJsonT[i]["dateCapture"],monJsonT[i]["valeur"]]);
            }
        }
        traitementDonneeCO2();
        traitementDonneeTemp();
        traitementDonneeHum();

        window.addEventListener('resize', function() {
            // Appeler la fonction reloadPage à chaque redimensionnement
            location.reload();
        });
        function goHome(){
            window.location.href = '/referent';
        }
    </script>
{% endblock %}
