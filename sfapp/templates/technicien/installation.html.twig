{% extends 'base.html.twig' %}


{% block title %}Installation{% endblock %}

{% block body %}
    <link rel="stylesheet" href={{ asset('tech_maintenance.css') }}>
    <div id="div_sa_info">
        <div class="imgANDtext">
            <span>  {{ curSA.name }} </span>
        </div>
        <div class="imgANDtext">
            {% if curSA.oldRoom is NULL %}
                <h2>Salle actuelle :</h2>
                <p>{{  cursa.oldRoom }}</p>
            {% endif %}
            <h2>A installer en :</h2>
            <span> {{ curSA.currentRoom.name }} </span>
        </div>
        <a href="{{ path("app_technicien") }}"><span>Retour</span></a>
    </div>
    <div id="block_ref_msg">
        <div id="ref_msg">
            <h2>Etapes de l'installation :</h2>
            <p>1 - Branchement du système d'acquisition</p>
            <p>2 - Configuration</p>
            <p>3 - Test des données remontées</p>
        </div>
    </div>
    {{ form_start(form_validInstal) }}
    {{ form_widget(form_validInstal.valid) }}
    {{ form_end(form_validInstal) }}
{% block title %}Espace Technicien{% endblock %}

{% block body %}
    {% extends 'base.html.twig' %}

    {% block title %}Espace Technicien{% endblock %}

    {% block body %}
        <section class="bandeau_tech">
        <div class="pres_page">

            <div class="buttonPresPage">
                <a id="button" href="{{ path("plan_salles") }}">Plan des salles</a>
            </div>
            <h1>Bonjour Technicien</h1>
            <p>Vous êtes sur l'espace technicien. Vous pouvez consulter l'ensemble de vos interventions et les valider.</p>
        </div>

        <section id="liste_interventions">
            <ul class="block_interv">
                <h1>Installations en attente</h1>
                <div>

                    {% for install in installations %}

                        <a id="view" href="{{ path('app_technicien', {'id': sa.getId}) }}">

                    {% endfor %}

                </div>
            </ul>
            <ul class="block_interv">
                <h1>SA disponibles</h1>
                <div>
                    {% for maintain in maintenances %}



                    {% endfor %}
                </div>
            </ul>
        </section>
        </section>{% endblock %}
{% endblock %}
